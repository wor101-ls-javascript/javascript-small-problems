let tweet = 'Woohoo! :-)';

if (tweet.length > 140) {           //length is not a function. it is a property that stores a number value representing the length of a string
  console.log('Tweet is too long!');
}